#include "world.h"
#include "graphics.h"
#include "object.h"



void initWorld()
{
	
}

bool isInsideWorld(Object* o)
{
	//
	return true;
	//return (x > 0 && x < getWindowWidth() && y > 0 && y < getWindowHeight());
}

